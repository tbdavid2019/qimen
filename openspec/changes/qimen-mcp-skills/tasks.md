## 1. Zero-Dependency Script

- [ ] 1.1 Create `mcp-bridge.js` using only Node.js built-in `https`, `readline`, and `process`.
- [ ] 1.2 Implement JSON-RPC 2.0 handler for `stdio` transport.
- [ ] 1.3 Add tool mappings for `qimen_ask` and `meihua_ask` (remote API calls).

## 2. Configuration & Discovery

- [ ] 2.1 Provide the `claude_desktop_config.json` snippet.
- [ ] 2.2 Verify the script runs and handles remote requests correctly.
